name: Deploy image to kubernetes

on:
  repository_dispatch:
    types: [kubernetes_deployment]

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: test-step
        run: |
          echo "hello world"
